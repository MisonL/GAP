# HuggingFace Spaces 优化配置示例
# 复制此文件为 .env 并根据需要修改

# === 必需配置 ===
# JWT密钥 - 必须设置强随机字符串
SECRET_KEY=your_very_strong_random_secret_key_here

# === API Key配置 ===
# 内存模式下必需的API Keys，用逗号分隔
GEMINI_API_KEYS=your_gemini_api_key_1,your_gemini_api_key_2

# === 认证配置 ===
# 平台用户登录密钥（用于 Web UI 登录），用逗号分隔多个
# 注意：这是平台自身的用户认证密钥，与 GEMINI_API_KEYS（AI API 调用密钥）不同
USERS_API_KEY=your_user_api_key_1,your_user_api_key_2

# === 存储模式 - HF Spaces免费层专用 ===
# 强制使用内存模式，禁用持久化存储
KEY_STORAGE_MODE=memory
CONTEXT_STORAGE_MODE=memory

# === 可选配置 ===
# 管理员API Key（可选）
ADMIN_API_KEY=your_admin_api_key_here

# 功能开关
ENABLE_NATIVE_CACHING=false
ENABLE_CONTEXT_COMPLETION=true

# 速率限制
MAX_REQUESTS_PER_MINUTE=60
MAX_REQUESTS_PER_DAY_PER_IP=600

# 其他设置
DISABLE_SAFETY_FILTERING=false